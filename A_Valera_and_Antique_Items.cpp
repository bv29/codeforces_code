#include<bits/stdc++.h>

using namespace std;

#define nline "\n"
typedef long long ll;


void solve(){

   ll n,v;
   cin>>n>>v;
   ll k, arr[k];

    for(ll i=0;i<n;i++){
        cin>>k;
         for(ll i=0;i<k;i++)
         {
            ll a;
            if(a<v){
                cout<<i;
            }
   }   
}
}

int32_t main() {
 ios_base::sync_with_stdio(false);
 cin.tie(NULL);

    int T = 1;
    // cin >> T;
    while (T--)
    {
        solve();
    }
    return 0;

}
